apiVersion: v1
kind: Service
metadata:
  name: k8s-mcp-server
  namespace: monitoring
  labels:
    app: k8s-mcp-server
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: k8s-mcp-server